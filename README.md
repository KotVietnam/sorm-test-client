# 🚀 DLP Traffic Generator (Client)

Клиентская часть ("Атакующий") для проверки DLP/DPI систем.
Запускается на рабочей станции инженера (Windows/Linux) и генерирует активность по заданным сценариям.

---

## 📋 Требования
1.  **Python 3.8+** (Обязательно добавьте Python в PATH при установке).
2.  **Google Chrome** (Установленный в системе).

---

## ⚙️ Установка и Настройка

### 1. Установка зависимостей
В папке с проектом выполните:
```bash
pip install -r requirements.txt
```
2. Настройка (ОБЯЗАТЕЛЬНО)

Откройте файл config.py в текстовом редакторе:

   Найдите переменную LAB_SERVER_IP.
   Впишите туда IP адрес вашего сервера из первого репозитория (dlp-lab-server).
   Python
   LAB_SERVER_IP = "192.168.1.50" # Пример
   (Опционально) Настройте ключевые слова в SECRET_DATA или параметры реальной почты.

###🎮 Как использовать
Вариант 1: Быстрый тест (quick_test.py)

Для чего: Проверка веб-фильтрации и интернет-каналов. Не требует сервера Docker. Что проверяет:

   HTTPS инспекцию (Google Search).
   Детекцию мессенджеров (WhatsApp, Telegram, Skype Web).
   Утечку данных через HTTP POST.

Запуск:
```bash
python quick_test.py
```

Вариант 2: Полный аудит (full_audit.py)

Для чего: Глубокая проверка корпоративных протоколов. Требует запущенный Docker-сервер. Что проверяет:
   FTP: Загрузка файла с меткой конфиденциальности.
   SMTP: Отправка внутренней почты.
   SIP: Имитация VoIP звонка и голосового RTP потока.
   Radius: Попытка авторизации.
   Telnet: Установка соединения.

Запуск:
```bash
python full_audit.py
```
